name: dfm_t140737
channels:
  - conda-forge
  - nodefaults
dependencies:
  - libnetcdf
  - zlib
  - proj <8.0
  - libtiff
  - gdal
  - libuuid
  - subversion
# and cmake if the system doesn't have a modern cmake installed.
# Note that environment creation may not use this path -- better to
# explicitly specify this on the commandline.
# prefix: /opt/software/delft/dfm/t140737
