name: dfm_t140737
channels:
  - conda-forge
  - nodefaults
dependencies:
  - libnetcdf
  - zlib
  - proj <8.0
  - libtiff
  - gdal
  - libuuid
  - subversion
# and cmake if the system doesn't have a modern cmake installed.
prefix: /opt/miniconda3/envs/dfm_t140737

